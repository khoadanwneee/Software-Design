| Nhóm chức năng                                  | Trạng thái hiện tại                                                                                                                   | Công nghệ nên dùng để tối ưu                                                                                        | Lý do                                                                                                                                           |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| **Realtime seat availability**                  | Chưa có WebSocket/SSE; chỉ có endpoint `/workshops/:id/seats`, UI chưa polling định kỳ.                                               | **SSE hoặc WebSocket**, **Redis cache/pub-sub**, fallback **short polling**                                         | `tasks.md` yêu cầu cập nhật số chỗ qua cache và WebSocket/SSE hoặc polling để sinh viên thấy số chỗ gần realtime sau đăng ký/hủy.               |
| **Search/filter workshop UI**                   | API đã có keyword/category, nhưng UI danh sách chưa có filter/search controls.                                                        | **React form state**, query params, debounce search, API pagination                                                 | Proposal yêu cầu sinh viên lọc theo ngày, phòng, chủ đề và trạng thái còn chỗ.                                                                  |
| **Room management đầy đủ**                      | Mới có API list/create; thiếu UI quản lý phòng, update/delete/status/layout upload.                                                   | **Admin React UI**, **Object Storage/local storage** cho sơ đồ phòng, PostgreSQL index theo room/status             | Tasks yêu cầu Organizer/Admin tạo/sửa/xem phòng, student chỉ đọc thông tin cần thiết.                                                           |
| **User/role management UI**                     | API admin xem users và đổi roles đã có, nhưng chưa thấy UI.                                                                           | **Admin Web UI**, **RBAC guard**, audit log                                                                         | Proposal yêu cầu RBAC cho STUDENT, ORGANIZER, CHECKIN_STAFF, ADMIN và admin quản lý quyền.                                                      |
| **Audit log phủ đủ nghiệp vụ**                  | Có audit cho workshop và đổi role; chưa phủ payment webhook, import, check-in override.                                               | **PostgreSQL audit_logs**, middleware/service audit, structured event payload                                       | Task 8.4 yêu cầu ghi audit cho admin action, payment webhook, CSV import và check-in override.                                                  |
| **Paid registration flow UI**                   | Backend tạo PENDING_PAYMENT và payment mock URL; UI chỉ hiện text Payment URL, chưa redirect/flow thật.                               | **Payment mock/sandbox redirect**, **Idempotency Key**, **PostgreSQL unique constraint**, **Circuit Breaker**       | Proposal yêu cầu thanh toán an toàn, không trừ tiền hai lần; tasks yêu cầu payment intent, idempotency và webhook idempotent.                   |
| **Payment reconciliation/timeout scheduler**    | Worker có xử lý `payment.reconcile`, nhưng chưa thấy scheduler/enqueue định kỳ.                                                       | **BullMQ repeatable job / cron worker**, PostgreSQL status scan, retry/backoff                                      | Task 3.5 yêu cầu payment PENDING quá lâu chuyển EXPIRED/NEEDS_REVIEW và không treo request.                                                     |
| **Refund/reversal**                             | Chưa có logic refund/worker/API/UI; schema mới có trạng thái.                                                                         | **Refund worker**, **state machine**, **Payment Gateway mock/sandbox**, audit log                                   | Task 3.7 yêu cầu đánh dấu refund khi workshop hủy hoặc paid nhưng không confirm seat.                                                           |
| **Offline validation bằng cache**               | Mới lưu offline event; chưa cache danh sách QR hợp lệ để validate offline thật.                                                       | **IndexedDB/SQLite local cache**, signed QR payload hoặc cached QR allowlist, background sync                       | Technical design mô tả mobile validate offline cơ bản bằng cache, lưu local event, sau đó batch sync để backend xử lý conflict.                 |
| **Staff assignment theo workshop/phòng**        | RBAC theo role đã có, nhưng chưa enforce staff chỉ scan workshop được phân công.                                                      | **Assignment table**, RBAC + ownership check ở service layer, audit log                                             | Technical design nêu CHECKIN_STAFF chỉ được sync check-in cho workshop được phân công.                                                          |
| **Notification in-app UI**                      | API/DB có notification, nhưng chưa có trang UI notification cho student.                                                              | **Student notification page**, unread count, polling/SSE tùy nhu cầu                                                | Task 5.5 yêu cầu student xem lịch sử notification của mình.                                                                                     |
| **Notification preferences/multi-channel**      | Chưa có preference, template engine, channel registry; email còn mock.                                                                | **notification_preferences**, **notification_deliveries**, channel interface, template engine, worker retry/backoff | Tasks yêu cầu schema preferences/deliveries, abstraction cho Email/App/future Telegram và retry policy.                                         |
| **AI summary thật từ PDF**                      | Mới mock summary; chưa upload binary PDF thật, object storage, extract/clean text, AI call thật.                                      | **Object Storage/local object storage**, PDF parser, worker queue, AI Service/mock AI Service, timeout/retry        | Proposal yêu cầu xử lý PDF để tạo AI summary; tasks 6.1–6.6 yêu cầu upload PDF, extract text, clean text, gọi AI service và lưu summary.        |
| **Student CSV import đầy đủ**                   | Có API tạo job từ `csvText`, worker parse/validate/upsert/log; thiếu upload file/object storage, parser mạnh, UI admin import/report. | **Object Storage**, robust CSV parser, worker job, PostgreSQL import job/row logs, admin report UI                  | Proposal yêu cầu CSV import hằng đêm có job log và xử lý lỗi từng dòng; tasks Phase 7 mô tả parser, validation, upsert, status và error report. |
| **Admin reporting nâng cao**                    | Dashboard cơ bản có registrations/checkins/revenue; UI chưa hiển thị circuit/recent imports đầy đủ.                                   | **Admin dashboard charts**, PostgreSQL aggregate queries/materialized views nếu cần, import/payment status widgets  | Proposal yêu cầu theo dõi số đăng ký, tham dự, no-show, doanh thu, trạng thái xử lý thanh toán, log import và audit log.                        |
| **Health checks tổng hợp**                      | Có `/health`, `/health/db`, `/health/redis`; thiếu broker/payment circuit health.                                                     | Health endpoint cho **PostgreSQL, Redis, Broker, Payment Gateway circuit state**                                    | Task 8.6 yêu cầu health check cho PostgreSQL, Redis, Broker và Payment Gateway circuit state.                                                   |
| **Structured logging / observability**          | Có request-id và morgan; chưa có JSON logs, metrics, tracing, alerting.                                                               | **Pino/Winston JSON logs**, request_id correlation, Prometheus metrics, OpenTelemetry tracing                       | Task 8.3 yêu cầu log có request_id, user_id nếu có, endpoint/job type.                                                                          |
| **Load test plan**                              | Chưa có `docs/load-test-plan.md` hoặc performance notes.                                                                              | **k6 hoặc Artillery**, scenario 12.000 users/10 phút, registration spike, p95/error/429/correctness metrics         | Task 8.7 yêu cầu tài liệu load test cho 12.000 users/10 phút, đo p95 latency, error rate, 429 rate và registration correctness.                 |
| **Integration tests thật cho luồng quan trọng** | Có test bị skip cho concurrency/integration.                                                                                          | **Vitest/Jest integration tests**, Testcontainers PostgreSQL/Redis, concurrency test                                | Task 2.8 yêu cầu test concurrency workshop 60 chỗ không vượt quá 60 registration CONFIRMED.                                                     |
