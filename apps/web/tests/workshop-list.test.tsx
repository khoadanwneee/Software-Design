import { describe, it } from "vitest";

describe.skip("Workshop list render", () => {
  it("renders workshops from TanStack Query with a mocked api client", () => {
    // Scaffold placeholder for MSW-based API mocking.
  });
});
